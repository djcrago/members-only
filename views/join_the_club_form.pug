extends layout 

block content
  h2= title

  p Enter the secret member passcode in order to become a member.

  form(method="post") 
    label(for="passcode") #[strong Passcode: ] 
    input#passcode(type="password" name='passcode' required)
    button(type='submit') Enter Passcode

  if errors 
    ul.errors
      for error in errors 
        li.error!= error.msg